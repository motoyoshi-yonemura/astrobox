# サイトの更新手順

サイトの中身を変更したら、以下の3つのコマンドを**この順番で**ターミナルに打ってください（`astrobox`ディレクトリの中で）。

```
git add .
git commit -m "変更内容"
git push
```

- 1行目: 変更したファイルをすべて記録の対象にする
- 2行目: `"変更内容"`の部分は、何を変えたかの簡単なメモ（例: `"自己紹介を追加"`）に書き換えてOK
- 3行目: GitHubに送信する

これでGitHubに変更が送られ、Netlifyが自動的に検知してビルド・公開してくれます。数十秒〜数分で本番サイト（`https://amazing-kringle-954f85.netlify.app`）に反映されます。

（`effulgent-clafoutis-ad533b.netlify.app`と`jolly-faun-a614ae.netlify.app`は過去に使っていた古いプロジェクトで、どちらも削除済みです。今後は上記の`amazing-kringle-954f85`だけを使ってください）

## 反映されたか確認する方法

1. https://app.netlify.com にログイン（`yomomobile@gmail.com`のアカウント）
2. `amazing-kringle-954f85` のプロジェクトを開く
3. 「Deploys」から最新のデプロイが「Published」になっているか確認

## お問い合わせフォームについて

送信データはNetlify Forms経由でWebhook（Google Apps Script）に送られ、`motomicro@gmail.com`が所有するGoogleスプレッドシート「astro_お問い合わせ」に自動記録されます（`yomomobile@gmail.com`は編集者として共有されています）。GASのWebアプリURLを再デプロイで変更した場合は、Netlify側の「Project configuration → Notifications → Form submission notifications」のURLも合わせて更新してください（「新しいデプロイ」ではなく「デプロイを管理」から既存デプロイを編集すればURLは変わりません）。

## うまくいかないとき

- `git push`でエラーが出た場合は、エラーメッセージをそのままAIに見せてください。
- 認証エラーが出た場合は、`gh auth login`は既に設定済みなので、通常は再入力不要です。
