# サイトの更新手順

プロジェクトの場所は `~/Projects/astrobox` です（2026-08-26に `~/Desktop/astrobox` から移動しました。iCloud同期による開発サーバーの不安定さを解消するためです）。

サイトの中身を変更したら、以下の3つのコマンドを**この順番で**ターミナルに打ってください（`astrobox`ディレクトリの中で）。

```
git add .
git commit -m "変更内容"
git push
```

- 1行目: 変更したファイルをすべて記録の対象にする
- 2行目: `"変更内容"`の部分は、何を変えたかの簡単なメモ（例: `"自己紹介を追加"`）に書き換えてOK
- 3行目: GitHubに送信する

これでGitHubに変更が送られ、**Cloudflare Workers**が自動的に検知してビルド・公開してくれます。数十秒〜数分で本番サイト（`https://astrobox.yomomobile.workers.dev`）に反映されます。

（過去に使っていたNetlifyのプロジェクトはすべて廃止し、現在はCloudflare Workersに一本化しています）

## 反映されたか確認する方法

1. https://dash.cloudflare.com にログイン（`yomomobile@gmail.com`のアカウント）
2. 左メニュー「Compute」→「Workers & Pages」→ `astrobox` を開く
3. 「Deployments」タブから最新のビルドが緑のチェックマーク（成功）になっているか確認

## 記事の書き方（Keystatic）

`npm run dev` でローカル開発サーバーを起動し、`http://localhost:4321/keystatic` を開くと、フォーム形式でブログ記事を編集できます（タイトル・説明文・公開日・タグ・本文）。保存すると `src/content/blog/` の中にMarkdownファイルとして書き込まれます。

**注意**: この編集画面はローカル専用です。本番サイト（Cloudflare Workers上）ではこの機能は使えません。記事を書いたら、いつも通り `git add` → `git commit` → `git push` で公開してください。

**ポートのずれについて（解消済み）**: 以前は`~/Desktop`にあったため、iCloud同期の影響でまれに`5173`番ポートに切り替わる問題がありましたが、プロジェクトを`~/Projects/astrobox`に移動したことで解消しました。今後は`localhost:4321`で安定するはずです。

## お問い合わせフォームについて

送信データは自前のAPIルート（`src/pages/api/contact.ts`）で受け取り、Google Apps Script経由で`motomicro@gmail.com`が所有するGoogleスプレッドシート「astro_お問い合わせ」に自動記録されます（`yomomobile@gmail.com`は編集者として共有されています）。GASのWebアプリURLを再デプロイで変更した場合は、`src/pages/api/contact.ts` 内の `GAS_URL` を新しいURLに書き換えてから、git push してください（「新しいデプロイ」ではなく「デプロイを管理」から既存デプロイを編集すればURLは変わらず、この作業は不要です）。

## うまくいかないとき

- `git push`でエラーが出た場合は、エラーメッセージをそのままAIに見せてください。
- 認証エラーが出た場合は、`gh auth login`は既に設定済みなので、通常は再入力不要です。
