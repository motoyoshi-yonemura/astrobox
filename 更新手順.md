# サイトの更新手順

サイトの中身を変更したら、以下の3つのコマンドを**この順番で**ターミナルに打ってください（`astrobox`ディレクトリの中で）。

```
git add .
git commit -m "変更内容"
git push
```

- 1行目: 変更したファイルをすべて記録の対象にする
- 2行目: `"変更内容"`の部分は、何を変えたかの簡単なメモ（例: `"自己紹介を追加"`）に書き換えてOK
- 3行目: GitHubに送信する

これでGitHubに変更が送られ、Netlifyが自動的に検知してビルド・公開してくれます。数十秒〜数分で本番サイト（`https://jolly-faun-a614ae.netlify.app`）に反映されます。

（`effulgent-clafoutis-ad533b.netlify.app`という古いURLは、Netlifyアカウントを切り替えた際にGitHubとの連携が途切れた別プロジェクトです。更新が反映されないので使わないでください）

## 反映されたか確認する方法

1. https://app.netlify.com にログイン（`motomicro@gmail.com`のアカウント）
2. `jolly-faun-a614ae` のプロジェクトを開く
3. 「Deploys」から最新のデプロイが「Published」になっているか確認

## うまくいかないとき

- `git push`でエラーが出た場合は、エラーメッセージをそのままAIに見せてください。
- 認証エラーが出た場合は、`gh auth login`は既に設定済みなので、通常は再入力不要です。
